{
  "updateIntervalMinutes": 30,
  "updateTerminalCommand": "kitty -e",
  "currentIconName": "world-download",
  "hideOnZero": true,
  "customCmdGetNumUpdates": "echo $(( $(dnf check-update -q | grep -v '^$' | grep -v '^Last metadata' | wc -l) + $(flatpak remote-ls --updates --columns=application | wc -l) ))",
  "customCmdDoSystemUpdate": "sudo nobara-sync cli"
}
